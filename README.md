# P/ Compilar o Typescript:
npx tsc ->  Gera um arquivo app.js
node app.js

# Observações

Precisa instalar o FFMPEG no sistema operacional.
Necessario compilar o Typescript antes de iniciar o docker compose.
